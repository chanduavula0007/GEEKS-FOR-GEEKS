git init
(used to initialize a git repo)

git add <file_name>
(adding file to staging)

git add .
(add all files / dir in current dir to staging)

git commit -m commit message

git reset head

git restore <file_name>
(get deleted file)
ls -a

cd "path"

git checkout <file_name> or git checkout .
(undo the changes)

git checkout <branch_name>
(create a branch)

git checkout -b <branch_name>
(create a branch)

git checkout <branch_name>>-
git switch <branch_name>
(switch to a branch)

git merge <branch_name>
(merge branch into the current branch)

gco -b feature 

git switch -c feature

git switch feature

git checkout -b feature

git fetch --all

git push

git log

git reflog

git remote add origin https://github.com/chanduavula0007/GEEKS-FOR-GEEKS.git

git push -u origin master

git chandu



